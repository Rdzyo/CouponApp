# Requirements:
Java 21, Docker

## How to start the application:
1. Run "docker compose up" command in cmd in main application folder
2. Wait for postgres to initialize on docker
3. Build and start the application through IDE
4. Test the APIs with swagger on [localhost:8080/swag](http://localhost:8080/swagger-ui/index.html#/)
